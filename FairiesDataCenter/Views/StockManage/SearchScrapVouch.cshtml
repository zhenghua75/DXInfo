@using Trirand.Web.Mvc
@model ynhnTransportManage.Models.StockManage.ScrapVouchGridModel
@{
    ViewBag.Title = "搜索库存不合格品记录单";
}
@Html.Trirand().JQGrid(Model.ScrapVouchGrid, "ScrapVouchGrid")
<script type="text/javascript">
$(document).ready(function () {
        $("#ScrapVouchGrid").closest("div.ui-jqgrid-view")
                .children("div.ui-jqgrid-titlebar")
                .css("text-align", "center")
                .css("font-size", "large")
                .children("span.ui-jqgrid-title")
                .css("float", "none");
    });
    function FormatNumber(num) {
        return parseFloat(num);
    }
    function UnFormatNumber(num) {
        return num;
    }
    function DblClick(rowid) {
        self.location = "@Url.Action("ScrapVouch", "StockManage")"+"?Id="+rowid;
    }
    function beforeRefresh() {
        $("#ScrapVouchGrid").jqGrid("setGridParam", {search: true}).trigger("reloadGrid", [{ page: 1}]);
    }
    function serializeGridData(postData){
        return postData;
    }
</script>