<UserControl x:Class="FairiesCoolerCash.Business.DataBaseBackupUserControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="200" d:DesignWidth="500"
	Background="{DynamicResource WindowBackgroundBrush}" >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Height="30">
            <Label>备份路径</Label>
            <TextBox x:Name="txtPath" Width="200"></TextBox>
            <Button Click="Button_Click" Width="100">选择路径...</Button>
            <Button Click="Button_Click_1" Width="100">备份</Button>
        </StackPanel>
        <ProgressBar x:Name="progessBar" Grid.Row="1" Height="50"></ProgressBar>
    </Grid>
</UserControl>
